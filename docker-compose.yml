services:
  backend-server:
    container_name: backend-server
    build:
      context: backend
      dockerfile: Dockerfile
    image: backend-server:latest
    ports:
      - 8080:8080
    networks:
      - global-network
networks:
  global-network:
    driver: bridge